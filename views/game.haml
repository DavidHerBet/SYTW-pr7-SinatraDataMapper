- if flash[:login]
  %p= flash[:login]
- if flash[:logout]
  %p= flash[:logout]
.screen
  .gameboard
    - HORIZONTALS.each do |row| 
      .gamerow
        - row.each do |p|
          %a(href=p)
            %div{:id => "#{p}", :class => "cell #{b[p]}"}
    .message
      %h1= m

